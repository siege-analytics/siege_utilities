# Sources

This directory contains non-code artifacts used for tests, demos, and historical reference.

- `tests/`: active lightweight fixture files used for local/package tests.
- `archive/`: historical one-off reports and presentation artifacts retained for traceability.

Do not add new one-off analysis exports to active fixture paths.
